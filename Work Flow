To do:
- stemming ( query, tittle, anchor, body)
- word2vec/ doc2vec
- Weights between indexes
- anchor, tittle - new indexes ( how to filter? )
- GloVe or fasttex as word2Vec
- query expansion
- doc2vec
- " If precision is more important than recall, then you might choose not to stem the words" by c.GPT
- Parameter to weight:
  bm25: b, k1
  indexes: anchor, body, tittle
  ranks: page view, page rank
  query expansion: how many tokens to add.
